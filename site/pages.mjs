import {profile,experience,earlierWork,projects,research,education,honors,leadership,skills,documents,repositorySnapshot,legacyHashes} from './content.mjs';
export const escapeHTML = (value='') => String(value).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const e = escapeHTML;
const arrow = '<span aria-hidden="true">↗</span>';
const link = (url,text,cls='text-link',external=false) => `<a class="${e(cls)}" href="${e(url)}"${external?' target="_blank" rel="noopener noreferrer"':''}>${e(text)} ${arrow}${external?'<span class="sr-only"> (opens in a new tab)</span>':''}</a>`;
const list = (items,cls='editorial-list') => `<ul class="${cls}">${items.map(item=>`<li>${e(item)}</li>`).join('')}</ul>`;
const kicker = (text) => `<p class="eyebrow">${e(text)}</p>`;
const sectionHead = (num,title,aside='') => `<div class="section-heading"><div>${kicker(num)}<h2>${e(title)}</h2></div>${aside}</div>`;
const pageHero = (section,title,intro) => `<header class="page-hero wrap">${kicker(section)}<h1>${title}</h1><p class="lead reading">${e(intro)}</p></header>`;
function image(photo,{className='',priority=false,sizes='(max-width: 700px) 90vw, 40vw',alt}={}) {
  if(!photo) return '';
  return `<img class="${e(className)}" src="${e(photo.src)}" srcset="${e(photo.srcset)}" sizes="${e(sizes)}" width="${photo.width}" height="${photo.height}" alt="${e(alt??photo.alt)}" loading="${priority?'eager':'lazy'}" decoding="async"${priority?' fetchpriority="high"':''}>`;
}
function nav(path) {
  const items=[['/work/','Work'],['/research/','Research'],['/life/','Life'],['/about/','About'],['/#contact','Contact']];
  const navLinks=items.map(([href,label])=>`<a href="${href}"${path.startsWith(href)&&href!=='/#contact'?' aria-current="page"':''}>${label}</a>`).join('');
  return `<a class="skip-link" href="#main">Skip to content</a><header class="site-header"><div class="wrap header-inner"><a class="wordmark" href="/" aria-label="Rishabh Chouhan, home">rishabh<span>.</span></a><nav class="desktop-nav" aria-label="Main navigation">${navLinks}</nav><details class="mobile-menu"><summary>Menu <span aria-hidden="true">+</span></summary><nav aria-label="Mobile navigation">${navLinks}${link('/archive/','Archive','menu-archive')}</nav></details></div></header>`;
}
function contact() {
  return `<footer id="contact" class="site-footer"><div class="wrap"><div class="contact-main"><div>${kicker('Start a conversation')}<h2>Have a problem<br>worth understanding?</h2></div><div class="contact-actions"><a class="email-link" href="mailto:${profile.email}">${profile.email} ${arrow}</a><button class="text-link copy-email js-only" type="button" data-email="${profile.email}">Copy email <span aria-hidden="true">⧉</span></button><p class="copy-status small" role="status"></p><p class="small">Atlanta, GA · Open to ideas worth building.</p></div></div><div class="footer-bottom"><a class="wordmark" href="/">rishabh<span>.</span></a><nav aria-label="Footer navigation">${link(profile.github,'GitHub','footer-link',true)}${link(profile.linkedin,'LinkedIn','footer-link',true)}${link(profile.reachmind,'Reachmind','footer-link',true)}${link('/archive/','Archive','footer-link')}${link('/documents/resume.pdf','Resume','footer-link',true)}</nav><p class="small">Built with curiosity.</p></div></div></footer>`;
}
function selectedWork() {
  return `<div class="work-features"><a class="work-feature" href="/work/kept/"><div class="work-art kept-art"><span class="art-note">01 / Institutional knowledge</span><div class="kept-word">Kept<span>.</span></div><p>What people know.<br>Why it matters.</p><span class="art-foot">Product direction · Engineering leadership</span></div><div class="feature-caption"><div><h3>Keeping context, not just information.</h3><p>Kept / Chief Engineering Officer</p></div><span class="circle-arrow" aria-hidden="true">↗</span></div></a><a class="work-feature" href="/work/care-access/"><div class="work-art care-art"><span class="art-note">02 / Healthcare & operations</span><p class="care-type">Connect.<br>Automate.<br>Make it reliable.</p><span class="art-foot">Systems · Workflows · Data</span></div><div class="feature-caption"><div><h3>Making complex operations work together.</h3><p>Care Access / AI Solutions Architect</p></div><span class="circle-arrow" aria-hidden="true">↗</span></div></a></div>`;
}
function musicPlayer(media,compact=false) {
  const tracks=compact?media.tracks.slice(0,1):media.tracks;
  if(!tracks.length) return '<p>No audio files are currently available.</p>';
  return `<div class="music-player" data-player>${kicker(compact?'A little of what I make':'The listening room')}<div class="player-heading"><h3 data-now-playing>${e(tracks[0].title)}</h3><span class="small">Songwriting & music</span></div><audio id="audio-player" controls preload="none" src="${e(tracks[0].src)}" aria-label="Music player">${link(tracks[0].src,'Open audio file')}</audio><p class="player-status small" role="status">Press play to listen.</p><button type="button" class="text-link js-only retry-audio" hidden>Retry playback</button><ol class="track-list">${tracks.map((track,i)=>`<li><span class="track-number" aria-hidden="true">${String(i+1).padStart(2,'0')}</span><button type="button" class="track-button js-only" data-track="${e(track.src)}" data-title="${e(track.title)}" aria-label="Play ${e(track.title)}">${e(track.title)} <span aria-hidden="true" class="play-symbol">▷</span></button><span class="no-js-only">${e(track.title)}</span><a href="${e(track.src)}" class="track-download" aria-label="Open audio file: ${e(track.title)}" download>Audio ${arrow}</a></li>`).join('')}</ol>${compact?link('/life/#music','All music'):''}</div>`;
}
function photoTile(photo,i) {
  return `<figure class="gallery-item"><a href="${e(photo.full)}" class="photo-link" data-lightbox data-caption="${e(photo.alt)}">${image(photo,{sizes:'(max-width: 560px) 92vw, (max-width: 900px) 45vw, 30vw'})}<span class="photo-enlarge" aria-hidden="true">↗</span></a><figcaption><span>${String(i+1).padStart(2,'0')} / ${e(photo.caption||'From the camera roll')}</span><a href="${e(photo.full)}" class="small" aria-label="Full image for photograph ${i+1}">Full image ${arrow}</a></figcaption></figure>`;
}
function home(media) {
  const photos=media.photos.filter(p=>p.name.toLowerCase().endsWith('.jpg')).slice(0,3);
  return `<section class="hero wrap" id="top"><div class="hero-copy">${kicker('Engineer. Researcher. Human.')}<h1>Rishabh<br>Chouhan<span class="accent">.</span></h1><p class="hero-statement">${e(profile.introduction)}</p><p class="hero-description">${e(profile.description)}</p><div class="hero-actions">${link('/work/','Explore my work','button primary')}${link('#contact','Get in touch','button quiet')}</div></div><figure class="hero-portrait">${image(media.portrait,{priority:true,sizes:'(max-width: 700px) 120px, (max-width: 1000px) 30vw, 360px'})}<figcaption><span class="location-dot" aria-hidden="true"></span> Based in Atlanta, Georgia.</figcaption></figure><div class="current-strip"><span class="eyebrow">Currently</span><a href="/work/care-access/">Care Access <span>AI systems & automation</span></a><a href="/work/kept/">Kept <span>Engineering leadership</span></a><a href="/research/">Georgia Tech <span>M.S. Computer Science & research</span></a></div></section>
  <section class="section wrap" id="work">${sectionHead('01 / Selected work','Ideas, put to work.',link('/work/','All work & projects'))}${selectedWork()}</section>
  <section class="research-home" id="research"><div class="wrap research-layout"><div>${kicker('02 / Research')}<h2>A little more context.<br><em>A better question.</em></h2></div><div><p class="research-question">${e(research.question)}</p><p class="reading">${e(research.summary)}</p><p class="small">${e(research.affiliation)} · ${e(research.status)}</p>${link('/research/','Explore the research')}</div></div></section>
  <section class="section wrap" id="life">${sectionHead('03 / Beyond the work','There’s more to the story.',link('/life/','Life & music'))}<div class="life-intro"><p class="lead reading">${e(profile.personal)}<br>Off hours, you’ll find me singing, writing songs, training, gaming, or somewhere new.</p></div><div class="photo-preview">${photos.map((p,i)=>photoTile(p,i)).join('')}</div><div class="home-music"><p class="music-aside">Some thoughts<br>sound better<br><em>as songs.</em></p>${musicPlayer(media,true)}</div></section>
  <section class="thread-section" id="about"><div class="wrap thread-layout">${kicker('04 / The thread')}<div><h2>Different interests.<br>The same curiosity.</h2><p class="lead reading">I keep returning to the same question: what changes when we understand more of the context?</p><p class="reading">It connects how I think about building systems, exploring research, and learning from people.</p>${link('/about/','A little more about me')}</div></div></section>
  <aside class="wrap archive-access" aria-label="Full portfolio archive"><p>Looking for the full picture?</p>${Object.entries(legacyHashes).map(([id,url])=>`<a id="${id}" href="${url}">${e({experience:'Experience',projects:'Projects',music:'Music',education:'Education',honors:'Honors & leadership',skills:'Skills',documents:'Documents'}[id])}</a>`).join('')}</aside>`;
}
function work() {
  return `${pageHero('01 / Work','Ideas, put<br>to work.','Engineering, automation, and research projects. The context behind the work, not just the tools used to build it.')}<section class="wrap section compact-top" aria-label="Featured work">${selectedWork()}</section><section id="projects" class="wrap section">${sectionHead('The project index','Experiments & builds.')}<div class="project-index">${projects.map((p,i)=>`<a class="project-row" href="/work/${p.slug}/"><span class="small">${String(i+1).padStart(2,'0')}</span><div>${kicker(p.category)}<h3>${e(p.title)}</h3><p>${e(p.description)}</p></div><span class="circle-arrow" aria-hidden="true">↗</span></a>`).join('')}</div></section><section class="wrap section" id="experience">${sectionHead('The background','Where I’ve worked.')}<div class="experience-list">${experience.map(job=>`<article class="experience-entry"><div><p class="small">${e(job.duration)}</p><h3>${e(job.company)}</h3><p class="role">${e(job.role)}</p></div><div><p>${e(job.summary)}</p>${job.bullets.length?`<details class="details"><summary>Responsibilities & technical detail</summary>${list(job.bullets)}</details>`:link('/work/kept/','Engineering at Kept')}</div></article>`).join('')}</div><p class="small earlier-work">${e(earlierWork)}</p></section><section class="wrap section" id="repositories">${sectionHead('Open source','The repository shelf.',link(profile.github+'?tab=repositories','See GitHub','text-link',true))}<p class="small">A public-repository snapshot from ${repositorySnapshot.asOf}. This is a curated index, not a live feed. GitHub is the source for current availability.</p><ul class="repository-list">${repositorySnapshot.names.map(name=>`<li>${link(profile.github+'/'+name,name,'repository-link',true)}</li>`).join('')}</ul></section>`;
}
function casePage(slug) {
  if(slug==='kept') return `${pageHero('Work / Kept','Keeping the context<br>that matters.','Kept is focused on preserving institutional knowledge and the context behind how people work.')}<section class="wrap case-meta" aria-label="Role details"><div>${kicker('My role')}<p>Chief Engineering Officer</p></div><div>${kicker('Focus')}<p>Institutional knowledge & decision context</p></div><div>${kicker('Scope')}<p>Engineering leadership</p></div></section><article class="wrap section case-body"><div>${kicker('The problem')}<h2>Information is only<br>part of the story.</h2></div><div class="reading"><p class="lead">A document can tell you what happened. It doesn’t always preserve why a decision was made, which assumptions mattered, or what someone learned along the way.</p><p>That distinction is the product direction behind Kept: preserving institutional knowledge alongside the context that makes it useful.</p><h3>Where I fit</h3><p>I lead engineering. My focus is the systems behind that direction: turning an idea about continuity into a product people can use.</p><h3>What this page represents</h3><p>This is a product and role overview. Detailed implementation decisions and measured results belong here when they can be shared with supporting evidence.</p>${link('/work/#experience','Full experience')}</div></article><section class="wrap case-next" aria-label="Next project">${link('/work/care-access/','Next: Care Access','next-link')}</section>`;
  if(slug==='care-access') return `${pageHero('Work / Care Access','Making the pieces<br>work together.','End-to-end automation and data systems across healthcare operations.')}<section class="wrap case-meta" aria-label="Role details"><div>${kicker('My role')}<p>AI Solutions Architect</p></div><div>${kicker('Dates')}<p>Jul 2025 – Present</p></div><div>${kicker('Focus')}<p>Automation · Data · Reliability</p></div></section><article class="wrap section case-body"><div>${kicker('The work')}<h2>From manual steps<br>to connected workflows.</h2></div><div class="reading"><p class="lead">My work connects the systems people use to get things done, with logging, diagnostics, and state tracking that make the automation easier to understand and maintain.</p><h3>Systems and responsibilities</h3>${list(experience[0].bullets)}<h3>The engineering throughline</h3><p>Integrations are only part of the work. Runbooks, clear interfaces between systems, and the handoff between operations, security, data, and engineering matter too.</p><p class="small">A responsibilities overview. Internal records and unverified performance metrics are not published here.</p></div></article><section class="wrap case-next" aria-label="Next project">${link('/work/diatrend/','Next: Diatrend','next-link')}</section>`;
  const p=projects.find(p=>p.slug===slug);
  if(!p) throw new Error('Unknown project: '+slug);
  return `${pageHero('Work / Project',e(p.title),p.focus)}<section class="wrap case-meta" aria-label="Project details"><div>${kicker('Area')}<p>${e(p.category)}</p></div><div>${kicker('Format')}<p>Technical project</p></div><div>${kicker('Explore')}${p.unavailable?'<p>Repository currently unavailable</p>':link(p.repo,'Source repository','text-link',true)}</div></section><article class="wrap section case-body"><div>${kicker('Project overview')}<h2>The idea &<br>the implementation.</h2></div><div class="reading"><p class="lead">${e(p.description)}</p><h3>Focus</h3><p>${e(p.focus)}</p><h3>Status & scope</h3><p>${e(p.status)}</p><p>Implementation details are available in the linked source where accessible.</p>${p.unavailable?`<details class="details"><summary>Original project reference</summary><p class="small">${e(p.repo)}</p></details>`:link(p.repo,'Explore the code','text-link',true)}${p.slug==='diatrend'?`<p>${link('/research/','Related research direction')}</p>`:''}</div></article><section class="wrap case-next" aria-label="More projects">${link('/work/#projects','Back to all projects','next-link')}</section>`;
}
function researchPage() {
  return `${pageHero('02 / Research','Better questions.<br>More context.','Exploring how state-aware processing can inform artificial pancreas loops.')}<section class="wrap research-thesis"><span class="status-tag">${e(research.status)}</span><h2>${e(research.question)}</h2><p class="small">${e(research.affiliation)} · Advised by ${e(research.advisor)}</p></section><section class="wrap section case-body"><div>${kicker('The direction')}<h2>Beyond the<br>observation alone.</h2></div><div class="reading"><p class="lead">${e(research.summary)}</p><p>The question is not only what a system observes, but what the underlying state might add to its interpretation.</p><h3>How to read this work</h3><p>This page describes a research direction. It does not present a published paper, validated clinical system, or proven improvement in treatment outcomes.</p><h3>Related technical work</h3><p>Diatrend explores CGM trajectory prediction with PyTorch, TabTransformer/GNN, and FastAPI.</p>${link('/work/diatrend/','Explore Diatrend')}<h3>Interested in the question?</h3><p>I’m open to thoughtful conversations about the problem, the approach, and the questions worth asking next.</p>${link('mailto:'+profile.email,'Start a conversation')}</div></section><section class="wrap section" aria-label="A working principle"><div class="research-principle">${kicker('A working principle')}<p>Be clear about what is observed,<br>what is inferred,<br><em>and what remains uncertain.</em></p></div></section>`;
}
function life(media) {
 return `${pageHero('03 / Life','Not everything<br>needs a brief.','Singing, songwriting, the gym, football, video games, and traveling. The parts that don’t fit neatly in a résumé.')}<section class="wrap section compact-top" id="music"><div class="life-music-layout"><div>${kicker('Music')}<h2>Some thoughts<br>sound better<br><em>as songs.</em></h2><p class="reading">${e(profile.personal)}</p><p class="small">Choose a track. Nothing plays until you press play.</p></div>${musicPlayer(media)}</div></section><section class="wrap section" id="photos">${sectionHead('The camera roll','Life, in between.')}<p class="small gallery-note">${media.photos.length} photographs and original-format variants. Open any image for a closer look.</p><div class="gallery-grid">${media.photos.map(photoTile).join('')}</div></section><dialog class="lightbox" aria-labelledby="lightbox-caption"><button class="lightbox-close" type="button" aria-label="Close photograph">Close <span aria-hidden="true">×</span></button><img class="lightbox-image" alt=""><p id="lightbox-caption"></p><a class="text-link lightbox-original" href="/life/">Open image ${arrow}</a></dialog>`;
}
function about(media) {
 return `${pageHero('04 / About','A little more<br>of the story.','I build AI systems for healthcare and operations — architecture, automation, and research. Based in Atlanta, GA.')}<section class="wrap section compact-top about-intro" aria-label="Introduction"><figure>${image(media.portrait,{sizes:'(max-width: 700px) 60vw, 360px',priority:true})}</figure><div><p class="lead">${e(profile.personal)}</p><p class="reading">My work brings together the practical side of building systems and the curiosity that makes research interesting. Outside of it, I make music, stay active, and look for new things to learn.</p><p class="reading">Off hours: ${e(profile.interests.charAt(0).toLowerCase()+profile.interests.slice(1))}</p><div class="hero-actions">${link('/work/#experience','Full experience','button primary')}${link('/documents/resume.pdf','Resume','button quiet',true)}</div></div></section><section class="wrap section" id="education">${sectionHead('The foundation','Education.')}<div class="education-list">${education.map(x=>`<article class="education-entry"><div><h3>${e(x.school)}</h3><p>${e(x.degree)}</p></div><p class="small">${e(x.note)}</p></article>`).join('')}</div></section><section class="wrap section" id="honors">${sectionHead('Recognition & community','Honors. Leadership.')}<div class="two-columns"><div><h3>Honors</h3>${list(honors)}</div><div><h3>Leadership</h3>${list(leadership)}</div></div>${link('/archive/','Supporting documents')}</section><section class="wrap section" id="skills">${sectionHead('The toolkit','Tools I work with.')}<ul class="skills">${skills.map(s=>`<li>${e(s)}</li>`).join('')}</ul><p class="small">The tools support the work. ${link('/work/','See them in context')}</p></section>`;
}
function archive(media) {
 const sizes=media.documents||{};
 return `${pageHero('05 / Archive','The supporting<br>material.','The full document shelf: résumé, academic awards, certifications, and recommendations.')}<nav class="wrap archive-nav" aria-label="Document groups">${documents.map((g,i)=>link('#group-'+i,g.title,'archive-nav-link')).join('')}</nav><section class="wrap section compact-top" id="documents" aria-label="Documents">${documents.map((g,i)=>`<section class="document-group" id="group-${i}"><h2>${e(g.title)}</h2><ul>${g.items.map(item=>`<li>${item.path?`<a href="${item.path}" target="_blank" rel="noopener noreferrer"><span>${e(item.label)}</span><span class="document-meta">PDF${sizes[item.path]?' · '+Math.ceil(sizes[item.path]/1024)+' KB':''} ${arrow}<span class="sr-only"> (opens in a new tab)</span></span></a>`:`<span>${e(item.label)}<small>Document not linked in the original portfolio.</small></span>`}</li>`).join('')}</ul></section>`).join('')}</section>`;
}
export function renderPages(media) {
 const pages = [
  {path:'/',title:'Rishabh Chouhan — AI systems, research & a little music',description:profile.introduction+' '+profile.description,body:home(media)},
  {path:'/work/',title:'Work — Rishabh Chouhan',description:'Engineering at Kept and Care Access, technical projects, experience, and a public repository index.',body:work()},
  {path:'/research/',title:'Research — Rishabh Chouhan',description:'Exploring state-aware processing in artificial pancreas loops. Research direction and related technical work.',body:researchPage()},
  {path:'/life/',title:'Life & music — Rishabh Chouhan',description:'Music, photography, and the personal side of Rishabh Chouhan.',body:life(media)},
  {path:'/about/',title:'About — Rishabh Chouhan',description:'Background, education, honors, leadership, skills, and interests.',body:about(media)},
  {path:'/archive/',title:'Archive — Rishabh Chouhan',description:'Résumé, awards, certifications, and recommendation documents.',body:archive(media)},
  ...['kept','care-access',...projects.map(p=>p.slug)].map(slug=>({path:'/work/'+slug+'/',title:(projects.find(p=>p.slug===slug)?.title||experience.find(p=>p.slug===slug)?.company)+' — Rishabh Chouhan',description:'Project and role overview, scope, and supporting context.',body:casePage(slug)})),
  {path:'/404.html',title:'Page not found — Rishabh Chouhan',description:'This page could not be found.',body:`${pageHero('404 / A wrong turn','Not here.<br>Still curious?','That page doesn’t exist. The work, research, and full archive are still here.')}<div class="wrap not-found-actions">${link('/','Back home','button primary')}${link('/archive/','Explore the archive','button quiet')}</div>`},
 ];
 return pages;
}
export function renderDocument(page,{css,js,noindex=false}) {
 const structured=JSON.stringify({'@context':'https://schema.org','@type':'Person',name:profile.name,url:profile.origin,email:profile.email,sameAs:[profile.github,profile.linkedin]}).replace(/</g,'\\u003c');
 return `<!doctype html>\n<html lang="en"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover"><meta name="theme-color" content="#f7f6f2"><title>${e(page.title)}</title><meta name="description" content="${e(page.description)}"><link rel="canonical" href="${profile.origin+page.path}"><meta name="robots" content="${noindex||page.path==='/404.html'?'noindex,follow':'index,follow'}"><meta property="og:type" content="website"><meta property="og:title" content="${e(page.title)}"><meta property="og:description" content="${e(page.description)}"><meta property="og:url" content="${profile.origin+page.path}"><meta property="og:image" content="${profile.origin}/media/social-preview.png"><meta property="og:image:width" content="1200"><meta property="og:image:height" content="630"><meta property="og:image:alt" content="Rishabh Chouhan — AI systems, research, and a little music"><meta name="twitter:card" content="summary_large_image"><meta name="twitter:title" content="${e(page.title)}"><meta name="twitter:description" content="${e(page.description)}"><meta name="twitter:image" content="${profile.origin}/media/social-preview.png"><link rel="icon" type="image/svg+xml" href="/favicon.svg"><link rel="stylesheet" href="${css}"><script type="application/ld+json">${structured}</script><script type="module" src="${js}"></script></head><body>${nav(page.path)}<main id="main" tabindex="-1">${page.body}</main>${contact()}</body></html>`;
}
